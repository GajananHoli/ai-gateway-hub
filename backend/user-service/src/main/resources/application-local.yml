spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aigatewayhub?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: aghuser
    password: aghpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true